import React from 'react'
import ProfileCard from './ProfileCard'

const Profile = () => {
    return (
        <section>
            <ProfileCard />
            {/* own events */}
        </section>
    )
}

export default Profile
